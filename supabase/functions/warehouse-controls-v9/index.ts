const code=String.raw`
(()=>{
 const JP='https://fujgwahltvbigyftpfjz.supabase.co/functions/v1/warehouse-jsonp';
 const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fa=n=>Number(n||0).toLocaleString('fa-IR');
 let user=null,records=[],allWorkers=[],filterTerm='',filterStatus='all',filterWorker='';
 const css='.v9-stage-box{margin-top:10px;padding:10px 12px;border:1px solid var(--vx-line,var(--line));border-radius:13px;background:var(--vx-soft,var(--card-2))}.v9-stage-row{display:grid;grid-template-columns:160px 1fr;gap:8px;align-items:end}.v9-hint{font-size:11px;color:var(--vx-muted,var(--muted));min-height:20px;padding:2px 2px 0}.v9-hint.exists{color:var(--vx-p,var(--primary));font-weight:800}.v9-doc{margin-top:10px;padding-top:10px;border-top:1px solid var(--vx-line,var(--line))}.v9-doc label{font-size:11px}.v9-doc-row{display:grid;grid-template-columns:1fr auto;gap:7px;align-items:center}.v9-doc-row input{min-height:42px!important}.v9-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.v9-actions button{width:auto!important;min-width:104px!important;min-height:42px!important;padding:8px 12px!important}.v9-delete{background:color-mix(in srgb,var(--vx-danger,var(--danger)) 10%,var(--vx-card,var(--card)))!important;color:var(--vx-danger,var(--danger))!important;border-color:color-mix(in srgb,var(--vx-danger,var(--danger)) 30%,var(--vx-line,var(--line)))!important}.v9-save-note{background:var(--vx-soft,var(--card-2))!important;color:var(--vx-p,var(--primary))!important;border-color:var(--vx-line,var(--line))!important}.v10-workers{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}.v10-worker{display:inline-flex;align-items:center;gap:5px;padding:5px 8px;border:1px solid var(--vx-line,var(--line));border-radius:999px;background:var(--vx-card,var(--card));font-size:11px;font-weight:800}.v10-worker b{color:var(--vx-p,var(--primary))}.v10-modal-bg{position:fixed;inset:0;z-index:9999;background:rgba(7,20,30,.58);display:flex;align-items:center;justify-content:center;padding:16px;backdrop-filter:blur(3px)}.v10-modal{width:min(440px,100%);max-height:90vh;overflow:auto;background:var(--vx-card,var(--card));color:var(--vx-text,var(--text));border:1px solid var(--vx-line,var(--line));border-radius:20px;box-shadow:0 24px 70px rgba(0,0,0,.28);padding:18px}.v10-modal h3{margin:0;font-size:18px}.v10-modal p{margin:5px 0 0;color:var(--vx-muted,var(--muted));font-size:12px}.v10-review{margin:14px 0;display:grid;gap:8px}.v10-review-row{display:flex;justify-content:space-between;gap:12px;padding:9px 10px;border-radius:12px;background:var(--vx-soft,var(--card-2));border:1px solid var(--vx-line,var(--line));font-size:12px}.v10-review-row span{color:var(--vx-muted,var(--muted))}.v10-review-row b{text-align:left}.v10-review-workers{display:grid;gap:6px;margin-top:6px}.v10-review-worker{padding:9px 10px;border-radius:12px;background:var(--vx-soft,var(--card-2));border:1px solid var(--vx-line,var(--line));display:flex;justify-content:space-between;gap:10px;font-size:12px;font-weight:800}.v10-existing{padding:10px 11px;border-radius:12px;background:color-mix(in srgb,var(--vx-warn,#9a5a14) 11%,var(--vx-card,#fff));color:var(--vx-warn,#9a5a14);font-size:12px;font-weight:800}.v10-modal-actions{display:grid;grid-template-columns:1fr 1.3fr;gap:8px;margin-top:14px}.v10-modal-actions button{min-height:48px!important}.v10-cancel{background:var(--vx-soft,var(--card-2))!important;color:var(--vx-text,var(--text))!important;border-color:var(--vx-line,var(--line))!important}.v10-success{text-align:center}.v10-check{width:68px;height:68px;border-radius:50%;display:grid;place-items:center;margin:2px auto 12px;background:color-mix(in srgb,var(--vx-ok,#17715f) 14%,var(--vx-card,#fff));color:var(--vx-ok,#17715f);font-size:34px;font-weight:900}.v10-success h3{font-size:22px}.v10-success .v10-review{text-align:right}.v10-worker-filter{min-width:155px!important;width:155px!important;min-height:42px!important;padding:8px 10px!important}.uiSearch.v10-search{display:grid!important;grid-template-columns:auto 1fr auto!important}.v10-confirm-label{font-size:13px;font-weight:900}.v10-registered-note{padding:7px 9px;border-radius:10px;background:color-mix(in srgb,var(--vx-ok,#17715f) 10%,var(--vx-card,#fff));color:var(--vx-ok,#17715f);font-size:11px;font-weight:900;margin-top:8px}@media(max-width:600px){.v9-stage-row,.v9-doc-row{grid-template-columns:1fr}.v9-actions button{flex:1;min-width:0!important}.v10-modal-actions{grid-template-columns:1fr}.uiSearch.v10-search{grid-template-columns:auto 1fr!important}.v10-worker-filter{grid-column:1/-1;width:100%!important;min-width:0!important}.v10-review-row{align-items:flex-start}.v10-review-row b{max-width:65%}}';
 let st=document.createElement('style');st.textContent=css;document.head.appendChild(st);
 function call(action,data={},auth=true){return new Promise((resolve,reject)=>{const token=localStorage.getItem('warehouse_token')||'',cb='v10'+Date.now()+Math.random().toString(36).slice(2),s=document.createElement('script');let done=false;const clean=()=>{if(done)return;done=true;try{delete window[cb]}catch{}s.remove()};window[cb]=j=>{clean();Number(j?.__status)>=400?reject(Object.assign(new Error(j.error||'خطا'),{data:j})):resolve(j)};s.onerror=()=>{clean();reject(new Error('ارتباط با سرور برقرار نشد'))};const p=new URLSearchParams({cb,action,payload:JSON.stringify(data)});if(auth&&token)p.set('token',token);s.src=JP+'?'+p.toString();document.body.appendChild(s);setTimeout(()=>{if(!done){clean();reject(new Error('پاسخ سرور دیر رسید'))}},16000)})}
 function notify(t){const m=$('message');if(m){m.innerHTML='<div class="msg">'+esc(t)+'</div>';setTimeout(()=>m.innerHTML='',4500)}else alert(t)}
 function ensureStageUI(){const inv=$('invoiceNo'),cus=$('customer');if(!inv||!cus||$('stageNo'))return;const box=document.createElement('div');box.className='v9-stage-box';box.innerHTML='<div class="v9-stage-row"><div><label for="stageNo">مرحله بارگیری</label><select id="stageNo">'+Array.from({length:15},(_,i)=>'<option value="'+(i+1)+'">مرحله '+fa(i+1)+'</option>').join('')+'</select></div><div><div id="invoiceHint" class="v9-hint">برای فاکتور جدید، مرحله ۱ ثبت می‌شود.</div></div></div>';const host=cus.closest('.input-grid')||cus.parentElement;host.after(box);let timer;const check=()=>{clearTimeout(timer);timer=setTimeout(checkInvoice,260)};inv.addEventListener('input',check);inv.addEventListener('blur',checkInvoice)}
 async function checkInvoice(){const inv=$('invoiceNo'),cus=$('customer'),stage=$('stageNo'),hint=$('invoiceHint');if(!inv||!stage)return;const no=inv.value.trim();if(!no){stage.value='1';if(cus)cus.readOnly=false;if(hint){hint.className='v9-hint';hint.textContent='برای فاکتور جدید، مرحله ۱ ثبت می‌شود.'}return}try{const x=await call('invoice_info',{invoice_no:no});if(x.exists){stage.value=String(x.next_stage);if(cus){cus.value=x.customer_name||cus.value;cus.readOnly=true}if(hint){hint.className='v9-hint exists';hint.textContent='این فاکتور قبلاً تا مرحله '+fa(x.last_stage)+' ثبت شده است — بارگیری جدید باید مرحله '+fa(x.next_stage)+' باشد.'}}else{stage.value='1';if(cus)cus.readOnly=false;if(hint){hint.className='v9-hint';hint.textContent='فاکتور جدید — ثبت به‌عنوان مرحله ۱.'}}}catch(e){if(hint)hint.textContent=e.message}}
 function workersPayload(){return [...document.querySelectorAll('.worker-line')].map(r=>{const s=r.querySelector('.wsel'),o=s?.options?.[s.selectedIndex];return {worker_id:s?.value||'',worker_name:o?.textContent?.trim()||'',amount:Number((r.querySelector('.wamt')?.value||'0').replace(/,/g,''))}}).filter(x=>x.worker_id&&x.amount>0)}
 function closeModal(el,result){el.remove();result?.()}
 function confirmRegistration(data,info){return new Promise(resolve=>{const bg=document.createElement('div');bg.className='v10-modal-bg';const workers=data.workers.map(w=>'<div class="v10-review-worker"><span>'+esc(w.worker_name)+'</span><b>'+fa(w.amount)+' تومان</b></div>').join('');const total=data.workers.reduce((s,w)=>s+w.amount,0);bg.innerHTML='<div class="v10-modal"><h3>تایید ثبت بارگیری</h3><p>قبل از ثبت نهایی، اطلاعات را یک‌بار بررسی کنید.</p><div class="v10-review"><div class="v10-review-row"><span>شماره فاکتور</span><b>'+esc(data.invoice_no)+'</b></div><div class="v10-review-row"><span>نام مشتری</span><b>'+esc(data.customer_name)+'</b></div><div class="v10-review-row"><span>مرحله</span><b>مرحله '+fa(data.stage_no)+'</b></div><div class="v10-review-row"><span>جمع دستمزد</span><b>'+fa(total)+' تومان</b></div><div><div class="v10-confirm-label">کارگرهای این مرحله</div><div class="v10-review-workers">'+workers+'</div></div></div>'+(info.exists?'<div class="v10-existing">این فاکتور قبلاً تا مرحله '+fa(info.last_stage)+' ثبت شده؛ ثبت جدید به‌عنوان مرحله '+fa(info.next_stage)+' انجام می‌شود.</div>':'')+'<div class="v10-modal-actions"><button type="button" class="v10-cancel">برگشت و اصلاح</button><button type="button" class="v10-final">تایید و ثبت نهایی</button></div></div>';document.body.appendChild(bg);bg.querySelector('.v10-cancel').onclick=()=>closeModal(bg,()=>resolve(false));bg.querySelector('.v10-final').onclick=()=>closeModal(bg,()=>resolve(true));bg.addEventListener('click',e=>{if(e.target===bg)closeModal(bg,()=>resolve(false))})})}
 function showSuccess(data,j){return new Promise(resolve=>{const bg=document.createElement('div');bg.className='v10-modal-bg';const workers=data.workers.map(w=>'<div class="v10-review-worker"><span>'+esc(w.worker_name)+'</span><b>'+fa(w.amount)+' تومان</b></div>').join('');bg.innerHTML='<div class="v10-modal v10-success"><div class="v10-check">✓</div><h3>ثبت شد</h3><p>مرحله بارگیری با موفقیت در سیستم ذخیره شد.</p><div class="v10-review"><div class="v10-review-row"><span>فاکتور</span><b>'+esc(data.invoice_no)+'</b></div><div class="v10-review-row"><span>مرحله ثبت‌شده</span><b>مرحله '+fa(j.stage_no)+'</b></div><div><div class="v10-confirm-label">کارگرها</div><div class="v10-review-workers">'+workers+'</div></div></div><div class="v10-registered-note">اطلاعات با موفقیت ذخیره شد و در سوابق قابل مشاهده است.</div><div style="margin-top:14px"><button type="button" class="v10-ok">باشه</button></div></div>';document.body.appendChild(bg);bg.querySelector('.v10-ok').onclick=()=>closeModal(bg,resolve)})}
 async function register(e){e.preventDefault();e.stopImmediatePropagation();const btn=$('registerBtn');if(!btn)return;try{const invNo=$('invoiceNo')?.value.trim()||'',ws=workersPayload();if(!invNo)throw new Error('شماره فاکتور را وارد کنید');if(!ws.length)throw new Error('حداقل یک کارگر و مبلغ معتبر وارد کنید');const info=await call('invoice_info',{invoice_no:invNo});let stageNo=Number(info.next_stage||1);if($('stageNo'))$('stageNo').value=String(stageNo);let customer=info.exists?(info.customer_name||''):($('customer')?.value.trim()||'');if(info.exists&&$('customer')){$('customer').value=customer;$('customer').readOnly=true}if(!customer)throw new Error('نام صاحب فاکتور را وارد کنید');const data={invoice_no:invNo,customer_name:customer,stage_no:stageNo,workers:ws,notes:$('notes')?.value.trim()||''};const approved=await confirmRegistration(data,info);if(!approved)return;btn.disabled=true;const j=await call('register_stage',{...data,workers:ws.map(w=>({worker_id:w.worker_id,amount:w.amount}))});await showSuccess(data,j);$('invoiceNo').value='';$('customer').value='';$('customer').readOnly=false;if($('notes'))$('notes').value='';if($('stageNo'))$('stageNo').value='1';if($('invoiceHint')){$('invoiceHint').className='v9-hint';$('invoiceHint').textContent='برای فاکتور جدید، مرحله ۱ ثبت می‌شود.'}document.querySelectorAll('.wamt').forEach(x=>x.value='');await refresh()}catch(err){if(err.data?.expected_stage&&$('stageNo'))$('stageNo').value=String(err.data.expected_stage);notify(err.message||'ثبت انجام نشد')}finally{btn.disabled=false}}
 function workersHtml(x){const ws=Array.isArray(x.workers)?x.workers:[];if(!ws.length)return '<div class="subtle" style="margin-top:7px">کارگر: ثبت نشده</div>';return '<div class="v10-workers">'+ws.map(w=>'<span class="v10-worker">کارگر: <b>'+esc(w.display_name||'')+'</b> · '+fa(w.amount)+' تومان</span>').join('')+'</div>'}
 function card(x){const inv=x.invoice?.invoice_no||'',customer=x.invoice?.customer_name||'',settled=!!x.settled_at,note=x.settlement_note||'',loadingNote=x.notes||'',manager=user?.role==='manager',workerIds=(x.workers||[]).map(w=>w.id).join(',');return '<div class="stage" data-stage-id="'+x.id+'" data-worker-ids="'+esc(workerIds)+'"><div class="stage-top"><div style="flex:1"><h4>فاکتور '+esc(inv)+' — '+esc(customer)+'</h4><div class="meta">مرحله '+fa(x.stage_no)+' · '+new Date(x.created_at).toLocaleString('fa-IR')+'</div><div class="amount">'+fa(x.amount_total)+' تومان</div><div class="tags">'+(settled?'<span class="tag badge ok">تسویه شده</span>':'<span class="tag badge">تسویه نشده</span>')+(x.creator?.display_name?'<span class="tag badge">ثبت: '+esc(x.creator.display_name)+'</span>':'')+'</div>'+workersHtml(x)+(loadingNote?'<div class="subtle" style="margin-top:7px">یادداشت بارگیری: '+esc(loadingNote)+'</div>':'')+(manager?'<div class="v9-doc"><label>یادداشت سند / تسویه</label><div class="v9-doc-row"><input class="field v9-note" maxlength="500" value="'+esc(note)+'" placeholder="شماره سند، توضیح پرداخت یا یادداشت"><button class="v9-save-note" type="button">ذخیره یادداشت</button></div><div class="v9-actions">'+(!settled?'<button class="v9-settle" type="button">✓ تسویه شد</button>':'')+'<button class="v9-delete" type="button">حذف</button></div></div>':(note?'<div class="subtle" style="margin-top:7px">یادداشت سند: '+esc(note)+'</div>':''))+'</div></div></div>'}
 function bindCards(){document.querySelectorAll('#stages .stage').forEach(el=>{const id=el.dataset.stageId,note=el.querySelector('.v9-note');el.querySelector('.v9-save-note')?.addEventListener('click',async()=>{try{await call('save_settlement_note',{stage_id:id,note:note?.value||''});notify('یادداشت سند ذخیره شد')}catch(e){notify(e.message)}});el.querySelector('.v9-settle')?.addEventListener('click',async()=>{if(!confirm('این مرحله به‌عنوان «تسویه شده» ثبت شود؟'))return;try{await call('settle_stage',{stage_id:id,note:note?.value||''});notify('تسویه با موفقیت ثبت شد');await refresh()}catch(e){notify(e.message)}});el.querySelector('.v9-delete')?.addEventListener('click',async()=>{if(!confirm('این مرحله از سوابق حذف شود؟ این کار برای حذف ثبت تکراری است.'))return;try{await call('delete_stage',{stage_id:id});notify('ثبت حذف شد');await refresh()}catch(e){notify(e.message)}})})}
 function setupFilters(){const search=$('uiSearchInp');if(search){search.placeholder=user?.role==='manager'?'شماره فاکتور، نام مشتری یا نام کارگر':'شماره فاکتور یا نام مشتری';search.oninput=e=>{filterTerm=e.target.value.trim().toLowerCase();filterRecords()};search.closest('.uiSearch')?.classList.add('v10-search')}document.querySelectorAll('[data-f]').forEach(b=>{b.onclick=()=>{filterStatus=b.dataset.f;document.querySelectorAll('[data-f]').forEach(x=>x.classList.toggle('on',x===b));filterRecords()}});if(user?.role==='manager'&&search&&!$('uiWorkerFilter')){const sel=document.createElement('select');sel.id='uiWorkerFilter';sel.className='v10-worker-filter';sel.innerHTML='<option value="">همه کارگرها</option>'+allWorkers.filter(w=>w.role==='worker').map(w=>'<option value="'+w.id+'">'+esc(w.display_name)+'</option>').join('');sel.onchange=()=>{filterWorker=sel.value;filterRecords()};search.closest('.uiSearch')?.appendChild(sel)}}
 function filterRecords(){document.querySelectorAll('#stages .stage').forEach(s=>{const txt=s.textContent.toLowerCase(),ok=!!s.querySelector('.tag.ok'),ids=(s.dataset.workerIds||'').split(',').filter(Boolean),passT=!filterTerm||txt.includes(filterTerm),passS=filterStatus==='all'||(filterStatus==='ok'&&ok)||(filterStatus==='un'&&!ok),passW=!filterWorker||ids.includes(filterWorker);s.style.display=passT&&passS&&passW?'':'none'})}
 async function refresh(){try{const j=await call('dashboard');records=j.stages||[];if($('sTotal'))$('sTotal').textContent=fa(j.summary.total)+' تومان';if($('sSettled'))$('sSettled').textContent=fa(j.summary.settled)+' تومان';if($('sUnsettled'))$('sUnsettled').textContent=fa(j.summary.unsettled)+' تومان';if($('stages'))$('stages').innerHTML=records.length?records.map(card).join(''):'<div class="empty">هنوز ثبتی وجود ندارد.</div>';bindCards();setupFilters();filterRecords()}catch(e){notify(e.message||'خطا در دریافت سوابق')}}
 function csv(e){e.preventDefault();e.stopImmediatePropagation();const rows=[['فاکتور','مشتری','مرحله','کارگرها','مبلغ','وضعیت','یادداشت سند'],...records.map(x=>[x.invoice?.invoice_no||'',x.invoice?.customer_name||'',x.stage_no,(x.workers||[]).map(w=>w.display_name+' ('+w.amount+')').join(' | '),x.amount_total,x.settled_at?'تسویه شده':'تسویه نشده',x.settlement_note||''])],out='\ufeff'+rows.map(r=>r.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([out],{type:'text/csv'}));a.download='گزارش-بارگیری.csv';a.click()}
 function boot(){ensureStageUI();const reg=$('registerBtn');if(reg&&!reg.dataset.v10){reg.dataset.v10='1';const sp=reg.querySelector('span');if(sp)sp.textContent='بررسی و تایید ثبت';reg.addEventListener('click',register,true)}const ref=$('refreshBtn');if(ref&&!ref.dataset.v10){ref.dataset.v10='1';ref.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();refresh()},true)}const c=$('csvBtn');if(c&&!c.dataset.v10){c.dataset.v10='1';c.addEventListener('click',csv,true)}const token=localStorage.getItem('warehouse_token');if(token&&!user){Promise.all([call('me'),call('list_workers')]).then(([x,w])=>{user=x.user;allWorkers=w.workers||[];setupFilters();refresh()}).catch(()=>{})}else if(user){setupFilters()}setTimeout(boot,900)}
 boot();
})();`;
Deno.serve(()=>new Response(code,{headers:{'content-type':'application/javascript; charset=utf-8','cache-control':'no-store','access-control-allow-origin':'*'}}));
